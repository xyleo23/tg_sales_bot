# TG Sales Bot — рассылка и поиск клиентов в Telegram

Бот в духе «Продажник Аі»: сбор аудитории из чатов, рассылка в личку с AI, управление аккаунтами, подписка.

## Быстрый старт

1. Скопируйте `.env.example` в `.env` и заполните:
   - `BOT_TOKEN` — от [@BotFather](https://t.me/BotFather)
   - `TG_API_ID` и `TG_API_HASH` — с [my.telegram.org](https://my.telegram.org) (для рассылки/парсинга через Telethon)

2. Установите зависимости и создайте таблицы:

```bash
cd tg_sales_bot
pip install -r requirements.txt
py -m scripts.create_tables
```

3. Запустите бота:

```bash
py -m bot.main
```

4. В Telegram: `/start` — главное меню. «⚡️ Подписка» — дата окончания триала. «Загрузить аккаунт» / «Аккаунты» — загрузка .session и список.

## Что уже есть

- **Фаза 1:** БД (User, Subscription), триал для новых, меню, раздел «Подписка», проверка доступа.
- **Фаза 2:** Аккаунты — загрузка .session, список, удаление (кнопка «🗑 Удалить»).
- **Фаза 3:** Парсер по участникам/сообщениям → аудитории; список с кнопкой «📥 Экспорт» (CSV).
- **Фаза 4:** Рассылка: выбор аудитории → аккаунты (ротация) → текст (плейсхолдеры {name}, {username}). Задержки настраиваются через .env (MAILING_DELAY_MIN/MAX).

- **Фаза 5:** Инвайтинг (аудитория → группа), прогрев аккаунтов, ссылка на оплату (`PAYMENT_LINK`).
- **Продакшн:** логи в файл (`LOG_FILE`), systemd, см. PRODUCTION.md.

Подробно: [plans/telegram_sales_bot_plan.md](../plans/telegram_sales_bot_plan.md).
